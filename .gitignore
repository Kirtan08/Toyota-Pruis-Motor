# Ignore everything by default...
*
# ...except folders (so files inside them can be matched), Python source files,
# Markdown files and this file
!*/
!*.py
!*.md
!.gitignore

# Never track compiled Python caches
__pycache__/
